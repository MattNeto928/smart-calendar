// This file serves as the entry point for Expo/React Native
import { registerRootComponent } from 'expo';
import App from './smart-calendar-mobile/App';

// Register the app
registerRootComponent(App);
